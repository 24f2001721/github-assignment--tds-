name: CI

on:
  push:
  pull_request:
  workflow_dispatch:    # lets you click "Run workflow" manually – very useful for testing!

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "Hello from GitHub Actions! Everything is fine 😊"
